@model IEnumerable<Category>

<li class="dropdown">
    <a class="nav-link text-dark" asp-controller="Book"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Book<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Book" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Book" asp-action="List" asp-route-Category="">View all books</a>
        </li>
    </ul>
</li>